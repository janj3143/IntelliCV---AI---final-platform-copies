# IntelliCV Master Requirements Guide
# This file provides guidance on installing the appropriate requirement sets
# Choose the combination that fits your deployment needs

# ========================================
# 🎯 BASIC DEPLOYMENT (Recommended Start)
# ========================================
# Essential functionality only - fastest installation
# Command: pip install -r requirements_core.txt
# Size: ~150MB | Time: ~3 minutes
# Includes: Streamlit, Pandas, Plotly, basic utilities

# ========================================  
# 🤖 AI-POWERED DEPLOYMENT
# ========================================
# Includes AI/ML capabilities for intelligent features
# Commands:
#   pip install -r requirements_core.txt
#   pip install -r requirements_ai.txt
# Size: ~3.7GB | Time: ~30 minutes
# Adds: OpenAI, Transformers, PyTorch, NLP libraries

# ========================================
# 🌐 FULL WEB PLATFORM
# ========================================
# Complete web application with APIs and data processing
# Commands:
#   pip install -r requirements_core.txt
#   pip install -r requirements_web.txt 
#   pip install -r requirements_data.txt
# Size: ~1GB | Time: ~25 minutes
# Adds: FastAPI, databases, file processing, web scraping

# ========================================
# 🛠️ DEVELOPMENT ENVIRONMENT
# ========================================
# Includes testing, linting, and development tools
# Commands:
#   pip install -r requirements_core.txt
#   pip install -r requirements_dev.txt
# Additional for full development:
#   pip install -r requirements_ai.txt
#   pip install -r requirements_web.txt
#   pip install -r requirements_data.txt

# ========================================
# 🏢 ENTERPRISE DEPLOYMENT
# ========================================
# Production-grade with monitoring, scaling, and enterprise features
# Requires infrastructure setup (Redis, PostgreSQL, etc.)
# Commands: All above + requirements_enterprise.txt
# Note: Requires additional server infrastructure

# ========================================
# 🐳 DOCKER DEPLOYMENT
# ========================================
# Use docker-compose.yml which includes core requirements
# Command: docker-compose up --build
# Automatically includes: Core + Web + Data processing

# ========================================
# 📋 INSTALLATION ORDER RECOMMENDATIONS
# ========================================

# 1. START HERE - Basic Platform:
#    pip install -r requirements_core.txt

# 2. ADD AI Features (if needed):
#    pip install -r requirements_ai.txt

# 3. ADD Web/API Features (if needed):
#    pip install -r requirements_web.txt

# 4. ADD Data Processing (if needed):
#    pip install -r requirements_data.txt

# 5. FOR DEVELOPMENT (optional):
#    pip install -r requirements_dev.txt

# 6. FOR ENTERPRISE (infrastructure required):
#    pip install -r requirements_enterprise.txt

# ========================================
# 🚨 IMPORTANT NOTES
# ========================================

# REMOVED INVALID PACKAGES:
# - python-email (use built-in 'email' module)
# - hashlib2 (use built-in 'hashlib' module)  
# - secrets (use built-in 'secrets' module)
# - sqlite3 (use built-in 'sqlite3' module)

# SYSTEM REQUIREMENTS:
# - Python 3.8+ (recommended 3.10+)
# - 8GB+ RAM for AI features
# - 10GB+ disk space for full installation
# - Git for development features

# VIRTUAL ENVIRONMENT RECOMMENDED:
# python -m venv venv
# source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate     # Windows
# pip install -r requirements_core.txt

# ========================================
# 🔗 QUICK LINKS
# ========================================
# Docker: docker-compose up --build  
# Streamlit: streamlit run admin_portal/pages/00_Home.py
# FastAPI: uvicorn main:app --reload
# Testing: pytest
# Documentation: Available in each requirements file